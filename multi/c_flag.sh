# python -c <command> executes <command> with the python interpreter, whoa
# and also you can delimit lines with semicolons 0_0
python -c 'import sys; print sys.argv[0]; print [x*x for x in (1, 2, 3)]'
